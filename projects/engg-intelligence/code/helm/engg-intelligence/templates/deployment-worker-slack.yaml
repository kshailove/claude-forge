apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "engg-intelligence.fullname" . }}-worker-slack
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "engg-intelligence.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker-slack
spec:
  replicas: {{ .Values.workers.slack.replicaCount }}
  selector:
    matchLabels:
      {{- include "engg-intelligence.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker-slack
  template:
    metadata:
      labels:
        {{- include "engg-intelligence.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker-slack
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: worker-slack
          image: {{ include "engg-intelligence.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - celery
            - -A
            - app.celery_app
            - worker
            - --queues={{ .Values.workers.slack.queues }}
            - --loglevel=info
            - --concurrency={{ .Values.workers.slack.concurrency }}
          envFrom:
            - secretRef:
                name: {{ include "engg-intelligence.secretName" . }}
          resources:
            {{- toYaml .Values.workers.slack.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
